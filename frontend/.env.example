# Produccion (Vercel):
# - Si usas vercel.json con rewrites /api y /uploads, deja vacio VITE_API_URL.
# - Si no usas rewrites, define VITE_API_URL con la URL publica del backend.
VITE_API_URL=

# Compatibilidad legacy (no recomendado en Vite, se mantiene por fallback)
# REACT_APP_API_URL=https://decoeclat.onrender.com
